<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Dashboard — RCP</title>
</head>
<body>
    <h1>Welcome, {{ first_name }}</h1>
    <p>Role: {{ role }}</p>
    <form method="POST" action="/logout">
        <button type="submit">Sign out</button>
    </form>
</body>
</html>